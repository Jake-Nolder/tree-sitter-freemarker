#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@maxbrunsfeld/flame-graph/bin/generate-flame-graph" "$@"
  ret=$?
else 
  node  "$basedir/../@maxbrunsfeld/flame-graph/bin/generate-flame-graph" "$@"
  ret=$?
fi
exit $ret
